# Repository Audit — SmartInventoryApplication

Student ids: 230103027,230103367,230103271,230103122

**Score: 7.5 / 10**

---

### README 
One line: `# smart-inventory-system`. No description, no setup instructions, no API overview. The `HELP.md` is auto-generated by Spring Initializr and contains nothing project-specific.

### Folder Structure 
Standard Gradle/Spring Boot layout followed correctly. Packages are cleanly separated by layer (`controller`, `service`, `repository`, `dto`, `entity`, `config`). One minor issue: the package is named `analitics` (typo) instead of `analytics`.

### File Naming 
Java class names are consistent PascalCase throughout. Deducted for the `analitics` package typo, which propagates through file paths and class names.

### Essential Files
| File | Status |
|---|---|
| `.gitignore` | ✅ Present (Spring default) |
| `build.gradle` | ✅ Present |


The `.env` file is not in `.gitignore` and is actively tracked. Both it and `application.properties` contain a live JWT signing secret and database password. **This must be fixed immediately** — rotate both secrets, add `.env` to `.gitignore`, and use `${ENV_VAR}` placeholders in `application.properties`.

### Commit Histor 
11 commits, all in the `"add X"` style with no context. Typos in messages (`"add product contoller"`, `"implemente all services"`). Large batches of work bundled into single commits. No branching, no conventional commit format.

---

### Top Fixes
1. 🚨 Remove `.env` from git, rotate secrets, use env var placeholders
2. Write a real `README.md` with setup and run instructions
3. Add `docker-compose.yml` for PostgreSQL
5. Fix the `analitics` → `analytics` package typo
